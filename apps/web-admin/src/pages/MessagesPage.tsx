import { FC } from "react";
import { PageTemplate } from "../components/PageTemplate";

export const MessagesPage: FC = () => {
    return <PageTemplate header="Messages">
        Messages
    </PageTemplate>
}